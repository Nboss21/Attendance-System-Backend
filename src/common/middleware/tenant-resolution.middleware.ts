import { Injectable, NestMiddleware } from '@nestjs/common'; import { Request, Response, NextFunction } from 'express'; import { TenantContextService } from '../tenant/tenant-context.service';
@Injectable() export class TenantResolutionMiddleware implements NestMiddleware { constructor(private readonly context: TenantContextService) {} use(req: Request & { user?: { companyId?: string }, tenant?: { companyId?: string } }, _res: Response, next: NextFunction) { const companyId = req.user?.companyId; req.tenant = companyId ? { companyId } : undefined; this.context.run(companyId, next); } }
