import { ForbiddenException, Injectable } from '@nestjs/common'; import { PrismaService } from '../../prisma/prisma.service'; import { TenantContextService } from '../../common/tenant/tenant-context.service'; import { CreateBranchDto } from './dto/create-branch.dto';
@Injectable() export class BranchesService { constructor(private readonly prisma: PrismaService, private readonly tenant: TenantContextService) {} private assertTenant(companyId: string) { if (this.tenant.companyIdOrThrow() !== companyId) throw new ForbiddenException('Cross-tenant access is not allowed'); } create(companyId: string, dto: CreateBranchDto) { this.assertTenant(companyId); return this.prisma.tenant().branch.create({ data: dto as any }); } findAll(companyId: string) { this.assertTenant(companyId); return this.prisma.tenant().branch.findMany({ orderBy: { name: 'asc' } }); } }
