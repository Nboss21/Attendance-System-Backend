import { ForbiddenException } from '@nestjs/common'; import { BranchesService } from './branches.service';
describe('BranchesService', () => { const scoped: any = { branch: { create: jest.fn(), findMany: jest.fn() } }; const prisma: any = { tenant: jest.fn(() => scoped) }; const tenant: any = { companyIdOrThrow: jest.fn(() => 'c1') }; const service = new BranchesService(prisma, tenant); beforeEach(() => jest.clearAllMocks()); it('creates a branch with the tenant-scoped Prisma client', async () => { scoped.branch.create.mockResolvedValue({ id: 'b1' }); await expect(service.create('c1', { name: 'HQ', timezone: 'Africa/Addis_Ababa' })).resolves.toEqual({ id: 'b1' }); expect(scoped.branch.create).toHaveBeenCalledWith({ data: { name: 'HQ', timezone: 'Africa/Addis_Ababa' } }); }); it('lists branches with the tenant-scoped Prisma client', async () => { scoped.branch.findMany.mockResolvedValue([]); await expect(service.findAll('c1')).resolves.toEqual([]); expect(scoped.branch.findMany).toHaveBeenCalledWith({ orderBy: { name: 'asc' } }); }); it('rejects a different tenant path id', () => { expect(() => service.findAll('c2')).toThrow(ForbiddenException); }); });
